{
  "name": "rss-feed",
  "private": true,
  "module": "index.ts",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter '*' dev",
    "build": "pnpm --filter @rss-feed/web build",
    "start": "cd packages/server && pnpm run start",
    "start:dev": "cd packages/server && pnpm run dev"
  },
  "devDependencies": {
    "@types/node": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
